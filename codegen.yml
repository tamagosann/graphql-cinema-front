overwrite: true
schema: "http://localhost:3001/graphql"
documents: "src/**/*.gql"
generates:
  src/graphql/generated.graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
